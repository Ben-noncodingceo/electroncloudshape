# 📸 Cloudflare Pages 设置图文指南

## 🎯 解决 "Missing entry-point to Worker script" 错误

你遇到的错误是因为 Cloudflare Pages 尝试运行 `npx wrangler deploy`，但这个项目是纯静态 HTML，不需要构建。

---

## 🔧 正确的配置步骤

### 方法一：首次部署时配置

#### 1. 连接 GitHub 仓库

1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 选择左侧 **Workers & Pages**
3. 点击 **Create application**
4. 选择 **Pages** 标签
5. 点击 **Connect to Git**
6. 授权并选择 `electroncloudshape` 仓库

#### 2. 配置构建设置（关键步骤！）

在 "Set up builds and deployments" 页面：

```
┌─────────────────────────────────────────────────────┐
│ Build settings                                      │
├─────────────────────────────────────────────────────┤
│                                                     │
│ Production branch                                   │
│ ┌─────────────────────────────────────────────┐   │
│ │ main                                        │   │
│ └─────────────────────────────────────────────┘   │
│                                                     │
│ Framework preset                                    │
│ ┌─────────────────────────────────────────────┐   │
│ │ None                                ▼       │   │ ← 选择 None
│ └─────────────────────────────────────────────┘   │
│                                                     │
│ Build command (optional)                            │
│ ┌─────────────────────────────────────────────┐   │
│ │                                             │   │ ← 留空！不要填任何内容
│ └─────────────────────────────────────────────┘   │
│                                                     │
│ Build output directory                              │
│ ┌─────────────────────────────────────────────┐   │
│ │ /                                           │   │ ← 填写一个斜杠 /
│ └─────────────────────────────────────────────┘   │
│                                                     │
│ Root directory (optional)                           │
│ ┌─────────────────────────────────────────────┐   │
│ │                                             │   │ ← 留空
│ └─────────────────────────────────────────────┘   │
│                                                     │
└─────────────────────────────────────────────────────┘
```

#### 3. 点击 "Save and Deploy"

Cloudflare 会开始部署，这次应该会成功！

---

### 方法二：修改现有项目配置（如果已经创建）

如果你已经创建了项目但部署失败：

#### 1. 进入项目设置

1. 在 Cloudflare Dashboard 中找到你的项目
2. 点击项目名称进入项目页面
3. 点击顶部的 **Settings** 标签

#### 2. 修改构建配置

1. 在左侧菜单选择 **Builds & deployments**
2. 找到 "Build configurations" 部分
3. 点击 **Edit configuration** 按钮
4. 修改以下设置：

```yaml
Framework preset: None
Build command: (完全删除，留空)
Build output directory: /
Root directory: (留空)
```

5. 点击 **Save** 保存

#### 3. 重新部署

1. 点击顶部的 **Deployments** 标签
2. 找到最新的失败部署
3. 点击右侧的 **⋮** 三个点
4. 选择 **Retry deployment**

或者：
1. 点击 **Create deployment** 按钮
2. 选择分支（main 或 claude/electron-cloud-visualizer-01WJpmtAhEYn18LShojqtEQR）
3. 点击 **Deploy**

---

## ✅ 验证配置正确

### 部署日志应该显示：

```
✅ Cloning repository...
✅ No build command specified. Skipping build step.
✅ Deploying your site to Cloudflare's global network...
✅ Success! Your site is deployed!
```

### 不应该看到：

```
❌ Executing user deploy command: npx wrangler deploy
❌ Missing entry-point to Worker script
```

---

## 🎯 关键点总结

### ✅ 正确配置

- **Framework preset**: `None`
- **Build command**: **完全空白**（不要有任何字符）
- **Build output directory**: `/`（一个斜杠）
- **Root directory**: 空白

### ❌ 错误配置

- **Framework preset**: ~~React, Vue, 或其他~~
- **Build command**: ~~`npm run build`, `npx wrangler deploy`~~
- **Build output directory**: ~~`./`, `dist`, `build`~~

---

## 🔍 为什么会出现这个错误？

### 原因分析

1. Cloudflare Pages 检测项目类型时，可能误判为需要构建的项目
2. 自动填充了默认的构建命令 `npx wrangler deploy`
3. 但这个项目是**纯静态 HTML**，所有依赖通过 CDN 加载
4. 不需要 npm, node_modules, 或任何构建工具

### 解决原理

- **Build command 留空** = 告诉 Cloudflare："这是静态文件，直接部署"
- **Build output directory = /** = 使用项目根目录的所有文件
- **Framework preset = None** = 不使用任何框架预设

---

## 📊 部署成功后

访问你的网站（例如 `https://electron-cloud-visualizer.pages.dev`）：

### 应该看到：

✅ 电子云可视化界面正常加载
✅ 3D 粒子系统正常渲染
✅ 2D 截面图正常显示
✅ 公式正确渲染
✅ 控制器可以交互

### 如果遇到其他问题：

- **页面空白**: 检查浏览器控制台（F12）
- **CDN 加载失败**: 检查网络连接
- **404 错误**: 检查 Build output directory 是否为 `/`

---

## 🆘 还是不行？

### 检查清单

- [ ] Build command 是**完全空白**（不是空格，是空的）
- [ ] Build output directory 是 `/`（斜杠）
- [ ] Framework preset 是 `None`
- [ ] 已经触发了重新部署
- [ ] 选择了正确的分支

### 联系支持

如果以上都确认无误但仍然失败，可以：

1. 查看完整的部署日志
2. 检查是否有网络问题
3. 尝试删除项目重新创建

---

## 🎉 预期结果

成功部署后：

- 🌐 你会得到一个 `.pages.dev` 域名
- 🔒 自动配置 HTTPS
- 🚀 全球 CDN 加速
- 📊 访问统计
- 🔄 Git push 自动部署

**部署时间**: 通常 30 秒 - 2 分钟

---

**祝部署成功！** 🚀

如果还有问题，请提供完整的部署日志，我会帮你分析。
